{
  "name": "@xeplr/expression-handler",
  "version": "2.0.0",
  "description": "The one formula engine for xeplr: its own parser (no eval, no dependencies), one function list, compiled-once formulas, totals over lists, AND/OR conditions. Runs in Node and the browser.",
  "main": "index.js",
  "files": [
    "index.js",
    "lib/"
  ],
  "scripts": {
    "test": "node --test"
  },
  "keywords": [
    "expression",
    "formula",
    "filter",
    "predicate",
    "rules",
    "evaluator",
    "xeplr"
  ],
  "author": "xeplr",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Xeplr/xeplr-expression-handler"
  },
  "publishConfig": {
    "access": "public"
  }
}
